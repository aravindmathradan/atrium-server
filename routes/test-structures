const express = require('express');
const router = express.Router();
const { getTestStructureMap } = require('../controllers/structures');

router.route('/')
  .get(getTestStructureMap);

module.exports = router;
